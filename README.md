# РљРѕСЂРїРѕСЂР°С‚РёРІРЅС‹Р№ Telegram-Р±РѕС‚ СѓС‡РµС‚Р° РїРёС‚Р°РЅРёСЏ

MVP-Р±РѕС‚ РЅР° `Node.js + Telegraf + PostgreSQL` РґР»СЏ СѓС‡РµС‚Р° РєРѕСЂРїРѕСЂР°С‚РёРІРЅРѕРіРѕ РїРёС‚Р°РЅРёСЏ РїРѕ Р°РІР°РЅСЃРѕРІРѕР№ СЃС…РµРјРµ:
- СѓС‡РµС‚ РєР»РёРµРЅС‚РѕРІ;
- РѕРїРµСЂР°С†РёРё `advance` (РѕРїР»Р°С‚Р°) Рё `meal` (РЅР°С‡РёСЃР»РµРЅРёРµ РїРёС‚Р°РЅРёСЏ);
- СЂР°СЃС‡РµС‚ РѕСЃС‚Р°С‚РєР° Р°РІР°РЅСЃР°;
- РёСЃС‚РѕСЂРёСЏ РѕРїРµСЂР°С†РёР№;
- РіРµРЅРµСЂР°С†РёСЏ РґРѕРєСѓРјРµРЅС‚РѕРІ:
  - Р°РєС‚ Р·Р° РјРµСЃСЏС† (`.docx`);
  - Р°РєС‚ СЃРІРµСЂРєРё (`.docx`);
  - СЂРµРµСЃС‚СЂ РїРёС‚Р°РЅРёСЏ (`.xlsx`).

## 1. Р§С‚Рѕ РґРµР»Р°РµС‚ РїСЂРѕРµРєС‚

Р‘РѕС‚ РІРµРґРµС‚ СѓС‡РµС‚ РїРѕ РєР»РёРµРЅС‚Р°Рј:
- РєР»РёРµРЅС‚ РІРЅРѕСЃРёС‚ Р°РІР°РЅСЃ;
- РїРѕ С„Р°РєС‚Сѓ РїРёС‚Р°РЅРёСЏ Р·Р°РЅРѕСЃСЏС‚СЃСЏ meal-РѕРїРµСЂР°С†РёРё;
- РѕСЃС‚Р°С‚РѕРє СЃС‡РёС‚Р°РµС‚СЃСЏ РєР°Рє `sum(advance) - sum(meal)`;
- Р·Р° РјРµСЃСЏС† РјРѕР¶РЅРѕ СЃС„РѕСЂРјРёСЂРѕРІР°С‚СЊ Р°РєС‚ Рё СЂРµРµСЃС‚СЂ;
- Р·Р° РїРµСЂРёРѕРґ РёР»Рё Р·Р° РІСЃСЋ РёСЃС‚РѕСЂРёСЋ РјРѕР¶РЅРѕ СЃС„РѕСЂРјРёСЂРѕРІР°С‚СЊ Р°РєС‚ СЃРІРµСЂРєРё.

Р”РѕСЃС‚СѓРї РѕРіСЂР°РЅРёС‡РµРЅ СЃРїРёСЃРєРѕРј `ADMIN_IDS`.

## 2. РЈСЃС‚Р°РЅРѕРІРєР° Р·Р°РІРёСЃРёРјРѕСЃС‚РµР№

```bash
npm install
```

## 3. РЎРѕР·РґР°РЅРёРµ Р‘Р”

РџСЂРёРјРµСЂ РґР»СЏ PostgreSQL:

```sql
CREATE DATABASE corp_settlements_bot;
```

## 4. РџСЂРёРјРµРЅРµРЅРёРµ SQL schema

Р’Р°СЂРёР°РЅС‚ A (С‡РµСЂРµР· СЃРєСЂРёРїС‚ РїСЂРѕРµРєС‚Р°):

```bash
npm run db:init
```

Р’Р°СЂРёР°РЅС‚ B (РІСЂСѓС‡РЅСѓСЋ С‡РµСЂРµР· psql):

```bash
psql -h localhost -U postgres -d corp_settlements_bot -f src/db/init.sql
```

## 5. РќР°СЃС‚СЂРѕР№РєР° .env

1. РЎРєРѕРїРёСЂСѓР№С‚Рµ `.env.example` РІ `.env`.
2. Р—Р°РїРѕР»РЅРёС‚Рµ `BOT_TOKEN`, `ADMIN_IDS` Рё РїР°СЂР°РјРµС‚СЂС‹ Р‘Р”.

РџРѕРґРґРµСЂР¶РёРІР°СЋС‚СЃСЏ РґРІР° РІР°СЂРёР°РЅС‚Р° РїРѕРґРєР»СЋС‡РµРЅРёСЏ:
- С‡РµСЂРµР· `DATABASE_URL`;
- С‡РµСЂРµР· `DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASSWORD`.

Р•СЃР»Рё Р·Р°РґР°РЅ `DATABASE_URL`, РѕРЅ РёСЃРїРѕР»СЊР·СѓРµС‚СЃСЏ РїСЂРёРѕСЂРёС‚РµС‚РЅРѕ.

## 6. Р—Р°РїСѓСЃРє Р±РѕС‚Р°

РџСЂРѕРґ:

```bash
npm start
```

Dev:

```bash
npm run dev
```

## 7. РћСЃРЅРѕРІРЅС‹Рµ С„СѓРЅРєС†РёРё РІ Р±РѕС‚Рµ

Р“Р»Р°РІРЅРѕРµ РјРµРЅСЋ:
- РљР»РёРµРЅС‚С‹
- Р”РѕР±Р°РІРёС‚СЊ Р°РІР°РЅСЃ
- Р”РѕР±Р°РІРёС‚СЊ РїРёС‚Р°РЅРёРµ
- РћСЃС‚Р°С‚РѕРє Р°РІР°РЅСЃР°
- РСЃС‚РѕСЂРёСЏ РѕРїРµСЂР°С†РёР№
- РЎС„РѕСЂРјРёСЂРѕРІР°С‚СЊ Р°РєС‚
- РЎС„РѕСЂРјРёСЂРѕРІР°С‚СЊ Р°РєС‚ СЃРІРµСЂРєРё
- РЎС„РѕСЂРјРёСЂРѕРІР°С‚СЊ СЂРµРµСЃС‚СЂ

РљРѕРјР°РЅРґС‹:
- `/start` - РѕС‚РєСЂС‹С‚СЊ РјРµРЅСЋ
- `/menu` - РїРѕРєР°Р·Р°С‚СЊ РјРµРЅСЋ
- `/cancel` - РѕС‚РјРµРЅРёС‚СЊ С‚РµРєСѓС‰РёР№ СЃС†РµРЅР°СЂРёР№

## РЎС‚СЂСѓРєС‚СѓСЂР° РїСЂРѕРµРєС‚Р°

```text
.
в”њв”Ђ src
в”‚  в”њв”Ђ app.js
в”‚  в”њв”Ђ config.js
в”‚  в”њв”Ђ bot
в”‚  в”‚  в”њв”Ђ index.js
в”‚  в”‚  в”њв”Ђ handlers.js
в”‚  в”‚  в””в”Ђ keyboards.js
в”‚  в”њв”Ђ db
в”‚  в”‚  в”њв”Ђ pool.js
в”‚  в”‚  в”њв”Ђ init.sql
в”‚  в”‚  в”њв”Ђ initDb.js
в”‚  в”‚  в””в”Ђ queries.js
в”‚  в”њв”Ђ services
в”‚  в”‚  в”њв”Ђ clientService.js
в”‚  в”‚  в”њв”Ђ operationService.js
в”‚  в”‚  в”њв”Ђ reportService.js
в”‚  в”‚  в”њв”Ђ actService.js
в”‚  в”‚  в”њв”Ђ reconciliationService.js
в”‚  в”‚  в””в”Ђ registryService.js
в”‚  в”њв”Ђ docs
в”‚  в”‚  в”њв”Ђ generateActDocx.js
в”‚  в”‚  в”њв”Ђ generateReconciliationDocx.js
в”‚  в”‚  в””в”Ђ generateRegistryXlsx.js
в”‚  в””в”Ђ utils
в”‚     в”њв”Ђ dates.js
в”‚     в”њв”Ђ money.js
в”‚     в””в”Ђ formatters.js
в”њв”Ђ generated
в”њв”Ђ .env.example
в”њв”Ђ .gitignore
в”њв”Ђ package.json
в””в”Ђ index.js
```

## РўРµСЃС‚РѕРІС‹Рµ РґР°РЅРЅС‹Рµ

РЎРєСЂРёРїС‚ `src/db/init.sql` РґРѕР±Р°РІР»СЏРµС‚ С‚РµСЃС‚РѕРІРѕРіРѕ РєР»РёРµРЅС‚Р°:
- `Р РµР№Р»С€РёРї` / `РћРћРћ В«Р РµР№Р»С€РёРї РЎРµСЂРІРёСЃВ»`
- РРќРќ `6658246043`, РљРџРџ `665801001`

Р С‚РµСЃС‚РѕРІС‹Рµ РѕРїРµСЂР°С†РёРё:
- `23.10.2025 advance 15000`
- `17.11.2025 meal 6700`
- `28.02.2026 meal 6100`

РћР¶РёРґР°РµРјС‹Р№ РѕСЃС‚Р°С‚РѕРє: `2200`.

